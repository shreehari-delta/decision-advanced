services:
  - type: web
    name: decision-hybrid-backend
    env: python
    rootDir: decision-query-engine/ml-backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false
